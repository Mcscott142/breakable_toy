<h1>Make an Offer</h1>

<%= simple_form_for [@listing, @offer] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :swap_listing_id, label: 'My Listings', collection: @listings %>
  <%= f.button :submit %>
<% end %>
